@model AT_System.Models._AT_Master

@{
    ViewBag.Title = "Cost Center Index";
}

<h2>Maintain Cost Center For Receiver Cost Center : @Html.DisplayFor(modelItem => Model.AT_RecCC.ReceiverCC)</h2>

<p>
    @Html.ActionLink("Add New Cost Center", "Create", new { id = Model.AT_RecCC.AutoRun }) | @Html.ActionLink("Back to List", "Index", "RecCC")
</p>

@*@using (Html.BeginForm())
{
    <table>
        <tr>
            <td>@Html.Label("Select Receiver Cost Center OR Emp ID:")</td>
            <td>@Html.TextBox("rec_cc")</td>
            <td><input type="submit" value="Search" name="Search" /></td>
        </tr>
    </table>
}*@

<div class="table-responsive"> 

<table class="table table-hover table-striped table-bordered">

@*            <td>@Html.HiddenFor(modelItem => item.FI03_AT_RecCostCenter)</td>
            <td>@Html.DisplayFor(modelItem => item.RecCC_NAME)</td>*@
@*        <th>FI03_AT_RecCostCenter</th>
        <th>RecCC_NAME</th>*@

    <thead class="tableHeader">
    <tr>
        <th>No.</th>
        <th>Asset Class</th>
        <th>Cost Center</th>
        <th>Room</th>
        <th></th>
    </tr>
    </thead>

    @{
    int no = 1;
    foreach (var item in Model.AT_CostCenter) 
    {
        <tr>

            <td>@(no++)</td>

            <td>@Html.DisplayFor(modelItem => item.AssetClass)</td>
            <td>@Html.DisplayFor(modelItem => item.CostCenter)</td>
            <td>
                @Html.DisplayFor(modelItem => item.Room)
                @Html.HiddenFor(modelItem => item.Client)
                @Html.HiddenFor(modelItem => item.AutoRun)
                @Html.HiddenFor(modelItem => item.Status)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.AutoRun }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.AutoRun })
            </td>

        </tr>
    }
}

</table>

</div>



@*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
@*<p>
    <input type="submit" value="Save" /> | @Html.ActionLink("Back to List", "Index")
</p>*@


